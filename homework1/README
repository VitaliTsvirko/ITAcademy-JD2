
# Конфигурации:
    ## Имена параметров
        Имена параметров GET задаются в файле web.xml
        Заданные значения:
        [Имя параметра конфигурации] = [имя параметра в GET запросе]
            FirstnameParamName = firstname
            LastnameParamName = lastname
            AgeParamName = age

        Хранилище данных задается в HTTP заголовке
        Имя параметра задается в файле web.xml
            HeaderDataStorageParamName = DataStorage

# Пример GET запроса:
http://localhost:8080/homework1-1.0.0-SNAPSHOT/Person?firstname=firstname&lastname=lastname&age=1

# Задание:
    Написать сервлет:
    1.	В этом сервлете, через заголовок, можно выбрать откуда брать данные из куки или сессий
    2.	Написать dto которое будет хранить фамилию, имя и возраст которое будет заполняться пользователем через параметры
    3.	Сохранять данные о пользователе в выбранное хранилище
    4.	Распечатать данные о пользователе